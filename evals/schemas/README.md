# Evaluation schemas

Canonical evaluation schemas live at the repository root: [`schemas/evaluation/`](../../schemas/evaluation/)

- `golden-case.schema.json`
- `run-manifest.schema.json`

This directory exists for future eval-specific schema additions (grading
records, evidence bundles). Nothing here may duplicate the canonical schemas.
